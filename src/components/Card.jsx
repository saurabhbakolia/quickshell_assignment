import React from 'react'

function Card({user, ticket}) {

    const {username, available} = user;
    const {id, title, tag, userId, status, priority} = ticket;

  return (
    <div className='card'>
        
    </div>
  )
}

export default Card